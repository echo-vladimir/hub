pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: pnpm run check --write
    types:
      run: pnpm run typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
